/**
 * Layer 1 – generated protobuf wrapper.
 *
 * Re-exports the proto namespace from @revizahoshii/hoshino. The protos are generated from
 * `.proto` definitions, not lib behavior, so direct reuse is safe.
 */

export { proto } from '@revizahoshii/hoshino/proto'
export type { Proto } from '@revizahoshii/hoshino/proto'
